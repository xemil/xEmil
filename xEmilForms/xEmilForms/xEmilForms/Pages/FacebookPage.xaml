<?xml version="1.0" encoding="utf-8" ?>
<mvvm:BaseView xmlns="http://xamarin.com/schemas/2014/forms"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:mvvm="clr-namespace:XLabs.Forms.Mvvm;assembly=XLabs.Forms"
               xmlns:controls="clr-namespace:XLabs.Forms.Controls;assembly=XLabs.Forms"
               x:Class="xEmilForms.Pages.FacebookPage"
               Title="TEST"
               BackgroundColor="#B455B">
  <!--<ActivityIndicator IsVisible="{Binding IsLoading}" IsRunning="{Binding IsLoading}"></ActivityIndicator>-->
  <StackLayout>
    <StackLayout>
      <Image Source="{Binding LoggedinFacebookUser.ProfileImage.Source}" HeightRequest="50" WidthRequest="50" Aspect="{Binding LoggedinFacebookUser.ProfileImage.Aspect}"></Image>
      <Label Text="{Binding LoggedinFacebookUser.FirstName}" VerticalOptions="Center" HorizontalOptions="Center"/>
      <Label Text="{Binding LoggedinFacebookUser.Gender}" VerticalOptions="Center" HorizontalOptions="Center"/>
    </StackLayout>
    <ListView ItemsSource="{Binding LoggedinFacebookUser.FriendList}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <ImageCell Text="{Binding Name}" Detail="Click to track" ImageSource="icon.png" Command="{Binding ClickedCommand}" CommandParameter="{Binding .}"></ImageCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </StackLayout>

</mvvm:BaseView>
